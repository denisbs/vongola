import urllib.request
import bs4
url = input("Digite o site da pesquisa")

request = urllib.request.urlopen(url)
document = request.read()

soup = bs4.BeautifulSoup(document)

for link in soup.findAll('a'):
        print (link.get('href'))
